# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0
python-multipart>=0.0.9
aiofiles>=24.0
httpx>=0.27.0

# Task Queue
celery[redis]>=5.4.0
redis>=5.0.0
flower>=2.0.0

# Video / CV
opencv-python>=4.9.0
# NumPy 2.x breaks many conda wheels (numexpr, bottleneck, sklearn) until rebuilt; cap for stability.
numpy>=1.26.0,<2.0
Pillow>=10.0
moviepy>=2.0.0
imageio>=2.9.0
imageio-ffmpeg>=0.4.0

# AI / Detection (optional; used for YOLO Tier 2 detector)
ultralytics>=8.3.0

# Audio
librosa>=0.10.2
soundfile>=0.12.0
pydub>=0.25.0
scipy>=1.12.0

# Config
pyyaml>=6.0
python-dotenv>=1.0.0

# AI Director (optional providers)
google-genai>=1.0.0
